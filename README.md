# speech-detection-project
This project finds where a person is speaking in an audio file. It separates speech from silence and saves the speech parts as new audio files with timestamps.
## How to Run

1. Place an audio file named input.wav in the project folder
2. Run the program:
   python main.py

## Output

- Displays speech timestamps
- Saves speech segments in the output folder
